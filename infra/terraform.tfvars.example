# AWS region where resources will be deployed
aws_region = "eu-west-2"

# Environment name
environment = "prod"

# Project name (used for resource naming)
project_name = "actions-dashboard"

# CloudFront price class (controls edge location distribution)
# PriceClass_All: All edge locations (most expensive)
# PriceClass_200: US, Canada, Europe, Asia, Middle East, Africa
# PriceClass_100: US, Canada, Europe (cheapest)
cloudfront_price_class = "PriceClass_100"

# Optional: Custom domain configuration
# Uncomment and set these to use a custom domain
# domain_name     = "actions.dashboard.stackgobrr.com"
# hosted_zone_id  = "Z03625632P0KL404I606"
# Note: ACM certificate will be automatically created and validated via DNS

# Optional: Store GitHub token in SSM Parameter Store
# github_token_parameter_name = "/github/actions-dashboard/token"
