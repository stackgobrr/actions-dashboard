# SSE Endpoint URL (from Terraform output: sse_handler_url)
# This should be set in your deployment environment
VITE_SSE_ENDPOINT=https://your-sse-lambda-url.lambda-url.region.on.aws/

# SEO Configuration
# Set to 'true' ONLY in production GitHub environment to allow search engine indexing
# Leave unset or set to 'false' in dev/staging to block indexing
VITE_ALLOW_INDEXING=true

# Plausible Analytics Domain
# The domain configured in your Plausible account
VITE_PLAUSIBLE_DOMAIN=actions.dashboard.stackgobrr.com

# GitHub App (server-side)
# Store these as secure environment variables in your deployment platform
## Actions Dashboard App (server-side)
# These are managed via AWS Secrets Manager and read by Lambda functions at runtime
# The workflow uploads these to Secrets Manager before deployment
# Numeric App ID
ACTIONS_DASHBOARD_APP_ID=
# Private key PEM contents (multi-line). Use your platform's secret manager for multiline values.
ACTIONS_DASHBOARD_APP_PRIVATE_KEY=
# Webhook secret configured in the GitHub App settings
ACTIONS_DASHBOARD_WEBHOOK_SECRET=

# OAuth client credentials (managed via AWS Secrets Manager)
ACTIONS_DASHBOARD_OAUTH_CLIENT_ID=
ACTIONS_DASHBOARD_OAUTH_CLIENT_SECRET=
# Note: ACTIONS_DASHBOARD_OAUTH_REDIRECT_URI is set automatically in Terraform based on domain_name
